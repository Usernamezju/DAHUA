# Runtime data, secrets, caches and experiment outputs
.env
.secrets/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
runtime/
artifacts/
work_dirs/
wandb/
*.log

# Datasets and local model assets remain in the project directory but are not
# committed to ordinary Git. Their exact hashes are recorded in
# models/MANIFEST.json; use Git LFS if they must be published.
data/
datasets/
dataset/
models/**/*.pth
models/**/*.pt
models/**/*.onnx
models/**/*.engine
models/**/*.rknn
models/**/*.task
!models/MANIFEST.json
!models/**/*.json
# Package build products written by scripts/package_models.py (kept out of
# the working tree by the dist/ staging, listed defensively here).
models/MANIFEST.*.json
models/SHA256SUMS.*
